# Data

Calibration and evaluation images are intentionally not tracked. Step 1 reads this directory by
default and deterministically selects 180 calibration images plus 60 test images. A different
directory may be provided with `--dataset`.

Only datasets with reviewed licenses and privacy status should be published.
