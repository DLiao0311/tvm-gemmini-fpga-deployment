# Data

Calibration and evaluation images are intentionally not tracked. Pass their location explicitly
to Step 1 with `--dataset`. The script deterministically selects 180 calibration images plus 60
test images from that directory.

Only datasets with reviewed licenses and privacy status should be published.
